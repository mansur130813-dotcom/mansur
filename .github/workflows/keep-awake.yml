name: Keep Supabase Awake

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase auth health
        run: |
          code=$(curl -sS -o /dev/null -w "%{http_code}" \
            -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind5cGNocnN2eW1ycm93aHRndHRuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI3MTcwMTQsImV4cCI6MjA5ODI5MzAxNH0.rOiBN9JmUPRQ1JXEK8IAvxJd-tr1CDzgipI8ugEGSVg" \
            "https://wypchrsvymrrowhtgttn.supabase.co/auth/v1/health")
          echo "HTTP status: ${code}"
          test "${code}" = "200"
